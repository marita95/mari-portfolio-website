source "https://rubygems.org"

# GitHub Pages builds the site automatically on push — this gem pins Jekyll and
# the plugins to exactly what GitHub Pages runs, so local previews match.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.0+ (no longer bundled with Ruby).
gem "webrick", "~> 1.8"
